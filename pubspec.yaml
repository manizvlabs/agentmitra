name: agent_mitra
description: Agent Mitra - LIC Agent Mobile App

publish_to: 'none'

version: 1.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI & Design
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.9
  flutter_screenutil: ^5.9.0
  lottie: ^3.0.0

  # State Management
  provider: ^6.1.1
  flutter_riverpod: ^2.4.9

  # Functional Programming
  dartz: ^0.10.1

  # Navigation
  go_router: ^13.2.0

  # Date/Time Utilities
  timeago: ^3.6.1

  # Networking
  http: ^1.1.0
  dio: ^5.4.0
  web_socket_channel: ^3.0.0

  # Configuration
  flutter_dotenv: ^5.1.0

  # Local Storage
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.0.0
  path_provider: ^2.1.3

  # Environment Variables (configured above)

  # Internationalization
  intl: ^0.20.2

  # Presentation Carousel
  carousel_slider: ^4.2.1

  # Image & Media
  cached_network_image: ^3.3.1

  # Rich Text Editing
  flutter_quill: ^8.1.1

  # Color Picker
  flutter_colorpicker: ^1.0.3

  # Reorderable Lists
  reorderables: ^0.4.3

  # Utilities
  uuid: ^4.2.1

  # Sharing
  share_plus: ^7.2.2

  # URL Launcher
  url_launcher: ^6.2.2

  # File Picker
  file_picker: ^8.0.0

  # Charts & Analytics
  fl_chart: ^0.70.0
  pdf: ^3.10.7
  csv: ^6.0.0

  # Feature Flags

  # Web-compatible alternatives
  # Note: Some mobile-specific features are disabled for web compatibility

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

  # Testing Framework
  patrol: ^3.0.0
  patrol_finders: ^2.0.0
  mockito: ^5.4.4

  # Code Generation
  freezed: ^2.4.7
  build_runner: ^2.4.8
  freezed_annotation: ^2.4.1
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/l10n/
    - .env

# Platform-specific dependencies
dependency_overrides:
  win32: ^5.15.0

# Platform-specific configurations
platforms:
  android:
    # Android-specific configurations can be added here
  ios:
    # iOS-specific configurations can be added here
  web:
    # Web-specific configurations can be added here
